name: CI
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
env:
  # Setting an environment variable with the value of a configuration variable
  env_var: ${{ vars.ENV_CONTEXT_VAR }}
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: install requirements
        run:  pip install -r requirements.txt
      - name: build html
        run: |
          cd ./docs
          make html
      - name: push to github.io
        env:
          SUPERSECRET: ${{ secrets.SUPERSECRET }}
          REPOSITORY_URL: ${{ vars.REPOSITORY_URL }}
          REPOSITORY_NAME: ${{ vars.REPOSITORY_NAME }}
        run: |
          git clone https://$env:SUPERSECRET@$env:REPOSITORY_URL
          mv -f build/html/* $env:REPOSITORY_NAME/
          cd $env:REPOSITORY_NAME
          git add *
          git commit -m "upd site"
          git push origin main
